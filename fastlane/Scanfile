# For more information about this configuration visit
# https://docs.fastlane.tools/actions/scan/#scanfile

# In general, you can use the options available
# fastlane scan --help

# Remove the # in front of the line to enable the option

# scheme("Example")

# open_report(true)

# clean(true)

# Enable skip_build to skip debug builds for faster test performance
skip_build(true)
project "MovieApp.xcodeproj"
derived_data_path "DerivedData"

# open_report true

# clean true

# devices ["iPhone 8"], # comment as contains bug: overrides the multi_scan parameter